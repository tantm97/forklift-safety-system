# Archived Changes

OpenSpec changes that have shipped. Each archived directory keeps the
workflow artefacts (`brief.md` / `proposal.md` / `spec.md` / `tasks.md`) for
historical reference. The persistent design lives in `../../docs/design/`.
