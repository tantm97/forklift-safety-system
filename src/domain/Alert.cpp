#include "forklift/domain/Alert.h"
// Translation unit kept so the linker has at least one symbol from Alert.h —
// helpful when we later add ULID generation, hashing, etc.
namespace forklift::domain {}  // namespace forklift::domain
